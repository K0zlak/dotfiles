
# Active vi mode with <Escepe>:
set -o vi
# Enviroment variables for Ranger
#shopt -s direxpand
#shopt -s dirspell
#shopt -s checkjobs
#shopt -s checkwinsize
#shopt -s cdspell

# Unbind  and 
bind -m vi-insert -r "\C-x"
bind -m vi-insert -r "\C-g"

# Allow you to cd into directoory marely by typing the directory name
shopt -s autocd


# Disable XON/XOFF flow control
stty -ixon
